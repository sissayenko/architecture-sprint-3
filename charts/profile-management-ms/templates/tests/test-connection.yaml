apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "profile-management-ms.fullname" . }}-test-connection"
  labels:
    {{- include "profile-management-ms.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "profile-management-ms.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
