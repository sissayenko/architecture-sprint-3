apiVersion: v1
kind: Service
metadata:
  name: {{ include "profile-management-ms.fullname" . }}
  labels:
    {{- include "profile-management-ms.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      nodePort: {{ .Values.service.nodePort }}
      protocol: TCP
      name: http
  selector:
    {{- include "profile-management-ms.selectorLabels" . | nindent 4 }}
