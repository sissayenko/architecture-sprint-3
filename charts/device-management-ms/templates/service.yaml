apiVersion: v1
kind: Service
metadata:
  name: {{ include "device-management-ms.fullname" . }}
  labels:
    {{- include "device-management-ms.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      nodePort: {{ .Values.service.nodePort }}
      protocol: TCP
      name: http
  selector:
    {{- include "device-management-ms.selectorLabels" . | nindent 4 }}
